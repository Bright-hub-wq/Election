@* @model IEnumerable<Election.Models.ElectionModel>

<div class="container mt-4">
    <h2>Elections</h2>

    @if (!Model.Any())
    {
        <p>No elections have been created yet.</p>
    }
    else
    {
        @foreach (var election in Model)
        {
            <div class="card mb-3">
                <div class="card-body">
                    <h4>@election.Title</h4>
                    <p>@election.Description</p>
                    <p>@election.StartDate.ToString("d") - @election.EndDate.ToString("d")</p>
         <h5>Assigned Candidates:</h5>
                    @if (election.ElectionCandidates.Any())
           
                    {
                        <ul>
                            @foreach (var candidate in election.ElectionCandidates.Select(ec => ec.Candidate))
                            {
                                <li>
                                    <img src="@candidate.PhotoPath" alt="@candidate.Name" style="width: 50px; height: 50px; margin-right: 10px;" />
                                    @candidate.Name (@candidate.Party)
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p>No candidates assigned to this election.</p>
                    }
                </div>
            </div>
        }
    }
</div>




 *@

@* 
 @model IEnumerable<Election.ViewModel.ElectionViewModel>

<div class="container mt-4">
    <h1 class="text-primary">Elections</h1>
    <div class="row">
        @foreach (var election in Model)
        {
            <div class="col-md-6 col-lg-4 mb-3">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title text-capitalize">@election.Title</h5>
                        <p class="card-text text-muted">@election.Description</p>
                        <p class="card-text"><strong>@election.StartDate.ToShortDateString()</strong> - <strong>@election.EndDate.ToShortDateString()</strong></p>
                        
                        <!-- Election Status Badge -->
                        <span class="badge @(
                            election.Status == "Ongoing" ? "bg-success" :
                            election.Status == "Ended" ? "bg-danger" : "bg-warning"
                        )">
                            @election.Status
                        </span>

                        <h6 class="mt-3">Assigned Candidates:</h6>
                        <ul class="list-unstyled">
                            @foreach (var candidate in election.Candidates)
                            {
                                <li class="d-flex align-items-center mb-2">
                                    <img src="@candidate.PhotoPath" class="rounded-circle me-2" style="width: 40px; height: 40px;" />
                                    @candidate.Name (@candidate.Party)
                                </li>
                            }
                        </ul>

                        <!-- Action Buttons -->
                        <div class="mt-3">
                            @if (election.Status == "Upcoming")
                            {
                                <a href="@Url.Action("EditElection", "Admin", new { id = election.Id })" class="btn btn-primary btn-sm">Edit</a>
                            }
                            @if (election.Status == "Ended")
                            {
                                <a href="@Url.Action("DeleteElection", "Admin", new { id = election.Id })" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this election?');">Delete</a>
                            }
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div> *@


@* 
 @model IEnumerable<Election.ViewModel.ElectionViewModel>
<div class="row row-cols-1 row-cols-md-2 g-4">
    @foreach (var election in Model)
    {
        <div class="col">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">@election.Title</h5>
                    <p class="card-text">@election.Description</p>
                    <p class="text-muted">@election.StartDate.ToShortDateString() - @election.EndDate.ToShortDateString()</p>
                    <p>
                        <span class="badge
                            @(election.Status == "Ongoing" ? "bg-success" :
                              election.Status == "Ended" ? "bg-danger" : "bg-warning")">
                            @election.Status
                        </span>
                    </p>
                    <h6>Assigned Candidates:</h6>
                    <ul class="list-unstyled">
                        @foreach (var candidate in election.Candidates)
                        {
                            <li class="mb-2">
                                <img src="@candidate.PhotoPath" alt="@candidate.Name" class="rounded-circle me-2" style="width: 40px; height: 40px;">
                                @candidate.Name (@candidate.Party)
                            </li>
                        }
                    </ul>
                </div>
                <div class="card-footer d-flex justify-content-between">
                    <form method="post" asp-action="DeleteElection" asp-route-id="@election.Id">
                        <button type="submit" class="btn btn-danger btn-sm" @(election.Status != "Ended" ? "disabled" : "")>
                            Delete
                        </button>
                    </form>
                    <a href="@Url.Action("EditElection", new { id = election.Id })" class="btn btn-primary btn-sm @(election.Status != "Upcoming" ? "disabled" : "")">
                        Edit
                    </a>
                </div>
            </div>
        </div>
    }
    }
</div>
 *@


 @model IEnumerable<Election.ViewModel.ElectionViewModel>

<div class="table-responsive">
    <table class="table table-bordered table-striped table-hover">
        <thead class="">
            <tr>
                <th>Election Title</th>
                <th>Description</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Status</th>
                <th>Candidates</th>
                <th>Party</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var election in Model)
            {
                <tr>
                    <td>@election.Title</td>
                    <td>@election.Description</td>
                    <td>@election.StartDate.ToShortDateString()</td>
                    <td>@election.EndDate.ToShortDateString()</td>
                    <td>
                        <span class="badge
                            @(election.Status == "Ongoing" ? "bg-success" :
                              election.Status == "Ended" ? "bg-danger" : "bg-warning")">
                            @election.Status
                        </span>
                    </td>
                    <td>
                        <ul class="list-unstyled">
                            @foreach (var candidate in election.Candidates)
                            {
                                <li>
                                    <img src="@candidate.PhotoPath" alt="@candidate.Name" class="rounded-circle me-2" style="width: 40px; height: 40px;">
                                    @candidate.Name
                                </li>
                            }
                        </ul>
                    </td>
                    <td>
                        <ul class="list-unstyled">
                            @foreach (var candidate in election.Candidates)
                            {
                                <li>@candidate.Party</li>
                            }
                        </ul>
                    </td>
                    <td>
                        <form method="post" asp-action="DeleteElection" asp-route-id="@election.Id" style="display: inline-block;">
                            <button type="submit" class="btn btn-danger btn-sm" @(election.Status != "Ended" ? "disabled" : "")>
                                Delete
                            </button>
                        </form>
                        <a href="@Url.Action("EditElection", new { id = election.Id })"
                           class="btn btn-primary btn-sm @(election.Status != "Upcoming" ? "disabled" : "")">
                            Edit
                        </a>
                    </td>
                </tr>
            }
            }
        </tbody>
    </table>
</div>

