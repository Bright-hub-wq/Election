@* @using Election.ViewModel
@model WinnerViewModel

<div class="card">
    <div class="card-header bg-primary text-white">
        <h3>Winner of "@Model.ElectionTitle"</h3>
    </div>
    <div class="card-body">
        <p><strong>Description:</strong> @Model.Description</p>
        <p><strong>Start Date:</strong> @Model.StartDate.ToShortDateString()</p>
        <p><strong>End Date:</strong> @Model.EndDate.ToShortDateString()</p>

        @if (!string.IsNullOrEmpty(Model.WinnerName))
        {
            <div class="text-center">
                <img src="@Model.WinnerPhotoPath" alt="@Model.WinnerName" class="rounded-circle" style="width: 150px; height: 150px;">
                <h4 class="mt-3">@Model.WinnerName</h4>
                <p><strong>Party:</strong> @Model.WinnerParty</p>
                <p><strong>Votes:</strong> @Model.WinnerVoteCount</p>
            </div>
        }
        else
        {
            <p class="text-danger text-center"><strong>No winner calculated for this election.</strong></p>
        }
    </div>
</div> *@
